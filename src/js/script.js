// const periodes = [
//   `kerstvakantie`,
//   `paasvakantie`
// ];

// const handleChangeFilter = (periode, $agenda) => {
//
//   $agenda.dataset.filter = periode;
//
// };

const init = () => {

  // console.log(`Hello, spekken`);
  // const $agenda = document.querySelector(`.agenda-items`);
  // const $select = document.querySelector(`select`);
  //
  // $select.addEventListener(
  //   `change`,
  //   e => handleChangeFilter(e.currentTarget.value, $agenda)
  // );
  //
  // periodes.forEach(periode => {
  //
  //   const $option = document.createElement(`option`);
  //   $option.value = periode;
  //   $option.innerText = periode;
  //   $select.appendChild($option);
  //
  // });

};

init();
